apiVersion: 1

datasources:
  - name: Loki
    uid: loki # fixed on purpose - dashboard JSON (observability/grafana/dashboards/*.json) references this literal uid
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
